<openerp>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="greenmobile.list">
      <field name="name">greenmobile list</field>
      <field name="model">greenmobile.greenmobile</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="greenmobile.action_window">
      <field name="name">greenmobile window</field>
      <field name="res_model">greenmobile.greenmobile</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="greenmobile.action_server">
      <field name="name">greenmobile server</field>
      <field name="model_id" ref="model_greenmobile_greenmobile"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="greenmobile" id="greenmobile.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="greenmobile.menu_1" parent="greenmobile.menu_root"/>
    <menuitem name="Menu 2" id="greenmobile.menu_2" parent="greenmobile.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="greenmobile.menu_1_list" parent="greenmobile.menu_1"
              action="greenmobile.action_window"/>
    <menuitem name="Server to list" id="greenmobile" parent="greenmobile.menu_2"
              action="greenmobile.action_server"/>
    -->
  </data>
</openerp>